{
  "name": "deep-goal",
  "version": "1.2.1",
  "type": "module",
  "private": true,
  "description": "Goal condition compiler for long-running autonomous work",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test",
    "verify:repo": "node scripts/verify-plugin.js",
    "verify": "npm run verify:repo && npm test"
  }
}
